//=========================================================================================

//npm install promp sync 

//=========================================================================================
const prompt = require('prompt-sync')()
/*
let nome = prompt("Qual o seu nome?")
let idade = prompt("Qual a sua idade?")
let cidade = prompt("Qual sua cidade?")

console.log(`Ola ${nome} voce tem ${idade} anos de idade e mora na cidade ${cidade} `)
/*

/*
=========================================================================================

node index.js
(OU O NOME DO ARQUIVO E PRECISA ESTAR DENTRO DA PASTA!!!!!)

=========================================================================================
*/

let bullyng = prompt ("O que é bullyng?")
let assimetria= prompt ("O que é assimetria de poder?")
let defina= prompt ("Defina Cyberbullyng:")
let Interseccionalidade= prompt ("O que é Interseccionalidade?")
let relação= prompt ("Qual a relação com o bullyng?")
let Kimberlé= prompt ("Quem foi Kimberlé Crenshaw?")
let preconceitos= prompt ("O que são preconceitos estruturais? Explique e citre 3 deles.")
let ocorre= prompt ("O bullying ocorre por acaso?")
let crime= prompt ("Bullying é crime?")

console.log(`É ${bullyng} Além disso, a assimetria de poder é descrita como ${assimetria}. No mundo digital, temos o ${defina}. Tudo isso se cruza com a ${Interseccionalidade}, que tem ${relação}. Esse conceito foi estudado por ${Kimberlé}, que nos ajuda a entender que ${preconceitos}. No fim, entendemos que o bullying não ${ocorre} e, legalmente, ${crime}.`)